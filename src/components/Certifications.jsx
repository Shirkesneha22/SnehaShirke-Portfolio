import { motion } from 'framer-motion';
import { FaAws, FaDatabase, FaPython } from 'react-icons/fa';
import '../styles/highlights.css';
const certifications = [{ title: 'Python', detail: 'Programming & application development', icon: <FaPython /> }, { title: 'SQL', detail: 'Relational database fundamentals', icon: <FaDatabase /> }, { title: 'AWS', detail: 'Cloud foundations & deployment', icon: <FaAws /> }];
export default function Certifications() { return <section id="certifications" className="section certifications"><div className="certifications-container"><h2 className="section-title">Certifications & Achievements</h2><p className="section-intro">Continuously strengthening my foundation in modern development, data, and cloud technologies.</p><div className="certifications-grid">{certifications.map((item, i) => <motion.article key={item.title} className="certification-card" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * .1 }}><span>{item.icon}</span><div><h3>{item.title}</h3><p>{item.detail}</p></div></motion.article>)}</div><div className="achievement-note"><strong>Currently working on:</strong> building AI-powered tools that turn resumes, documents, and data into practical insights.</div></div></section>; }
